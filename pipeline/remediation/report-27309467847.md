# Remediation Report — Run 27309467847 (Load Testing)

- **Date:** 2026-06-10
- **Workflow:** Load Testing — job `Integration Load Tests`, step `Run database migrations`
- **Head SHA:** 91f8e370745c0262ad656e8569c685f731afadca
- **Attempt:** 2 of 3 in window
- **Outcome:** Already fixed on main — no code change needed.

## Symptom

Migration runner failed at `182_embedding_dimension_768.sql`:

```
MIGRATION_ERROR file=182_embedding_dimension_768.sql
error=cannot drop column embedding of table iris_entities because other objects depend on it
```

## Root cause

At SHA 91f8e37, migration 182 ran `ALTER TABLE iris_entities DROP COLUMN IF EXISTS embedding;`
**without `CASCADE`**, while the compat views `indexed_entities` and `entity_vectors`
(migration 180) reference that column — Postgres rightly refuses the drop.

## Why no fix is pushed

The root cause was already fixed in `c94d2ce`
(`fix(api): migration 182 — keep vector(1536) default and CASCADE drop to rebuild compat views`),
which adds `CASCADE` to the column drop and recreates both compat views in the same migration.

Verified the fix held: Load Testing runs on every subsequent SHA are green —
`27310178859` (c94d2ce), `27311374646` (4e03524), `27311682775` (1983681),
`27311778016` (92adb40) all `success`.

Run 27309467847 executed against the pre-fix SHA; this is a stale failure event.
Sibling stale runs 27309678793 and 27309678797 were already closed out the same way
(reports in commits 92adb40 and 4907193).

## Suggested next probe (if this recurs on a post-c94d2ce SHA)

A recurrence on a newer SHA would mean a *different* dependent object (not the two
compat views) references `iris_entities.embedding`. Probe with:
`SELECT dependent_view.relname FROM pg_depend ... ` or simply read the new
`MIGRATION_ERROR` line — the CASCADE in 182 should otherwise make this impossible.
