# Remediation Report — Run 27309176168 (Slice Demo)

- **Date:** 2026-06-10
- **Workflow:** Slice Demo
- **Failed step:** Vertical Slice Demo → "Run slice demo (run 1 of 2)"
- **Head SHA:** `0b85cfaafb24535ab4a5398c468ec03362435216`
- **Attempt:** 3 of 3 in current window
- **Outcome:** No code change — stale failure on a pre-fix SHA. Root cause already fixed in `c94d2ce`.

## Symptom

From the `slice-report` artifact (`slice-demo-run1.log`, via nightly.link):

```
MIGRATION_ERROR file=182_embedding_dimension_768.sql error=cannot drop column embedding of table iris_entities because other objects depend on it
SLICE DEMO FAILED: Migrations exited with code 1
```

Migration 182 attempted `DROP COLUMN embedding` on `iris_entities` while the
compat views created by `180_indexed_entities_compat_views.sql` still
referenced the column.

## Why no fix was pushed

- `git merge-base --is-ancestor` confirms head SHA `0b85cfa` **predates** fix
  commit `c94d2ce` (`fix(api): migration 182 — keep vector(1536) default and
  CASCADE drop to rebuild compat views`), and the fix is **not** contained in
  the failed SHA.
- Current `main` already carries the fix:
  `ALTER TABLE iris_entities DROP COLUMN IF EXISTS embedding CASCADE;` followed
  by recreation of the compat views.
- This is the **third** remediation event for the same pre-fix SHA (see
  `report-27309467847.md`, `report-27309467859.md`, prior report for
  27309176061). All are queued/stale runs that started before `c94d2ce` landed.

## Suggested next probe (if a 4th event fires)

- If a new Slice Demo failure arrives with head SHA at or after `c94d2ce` and
  the same `MIGRATION_ERROR` on 182, the CASCADE fix is insufficient in CI —
  diagnose fresh from the new artifact.
- Consider having the watcher (`scripts/ci-remediator-watch.py`) skip
  remediation events whose head SHA is an ancestor of a commit already cited as
  the fix in `pipeline/remediation/` reports, to stop burning attempts on stale
  queue backlog.
