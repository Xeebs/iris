# Remediation report — run 27311993765 (Onboarding Golden Path)

- **Run:** 27311993765, head SHA `bbf442f` (attempt 2 of 3 in window)
- **Failed step:** "Run onboarding golden path tests" in job "Onboarding API Tests (real API, no mocks)"
- **Verdict:** Stale failure on a pre-fix SHA. Root cause already fixed and verified by attempt 1 (`3792b5b`, remediation for sibling run 27312069943). No new fix pushed.

## Diagnosis

1. Annotations on `bbf442f` check runs carried only the Node 20 deprecation notice and
   "Process completed with exit code 1" — no actionable detail.
2. The run produced **zero artifacts**: at `bbf442f` the workflow uploaded only
   `tests/playwright-report/`, and `--reporter=list` never writes that directory,
   so failures were undiagnosable from the build host (gh token cannot read Actions logs).
3. Sibling run 27312069943 (same workflow, same step, later SHA `e4562ca`, also pre-fix)
   was remediated in attempt 1 by local reproduction on the Pi (native Postgres + Redis).
   Root causes found and fixed in `3792b5b`:
   - `GET /api/v1/connectors/types` sat behind auth, but onboarding lists connector
     types before a workspace API key exists → extracted `createConnectorTypeRoutes()`
     and mounted it publicly in `server.ts`.
   - `GET /api/v1/connectors/:id/health` returned 503 `NOT_CONFIGURED` →
     wired `ConnectorHealthService(sql)` plus a live health check fallback.
   - Instrumentation: test step output now teed to `tests/onboarding-run.log` and
     uploaded via an `if: always()` artifact, so future failures are diagnosable.
4. Attempt 1 verified locally: tsc clean, connectors unit tests 37/37, full
   onboarding-golden-path suite 4/4 against a real API + fresh Postgres.
5. The Onboarding Golden Path run on `3792b5b` was **in progress** at the time of this
   remediation. Per the no-polling rule, this session does not wait for it; if it fails,
   the watcher fires attempt 3 with a diagnosable `onboarding-run.log` artifact.

## Action taken

Report only. Pushing a second speculative fix on top of an in-flight, locally verified
fix would risk conflicting changes for no information gain.
