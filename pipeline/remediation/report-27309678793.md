# Remediation Report — Run 27309678793 (Slice Demo)

- **Date:** 2026-06-10
- **Head SHA:** 42a8de17078b0c95364d7d75fecfc965346d8743
- **Failed step:** Vertical Slice Demo → Run slice demo (run 1 of 2)
- **Attempt:** 1 of 3

## Root cause

Same defect as run 27309678797 (Load Testing, same SHA — see `report-27309678797.md`).
Migration `apps/api/migrations/182_embedding_dimension_768.sql` at the failing SHA dropped
`iris_entities.embedding` **without `CASCADE`** while the compat views from migration 180
(`indexed_entities`, `entity_vectors`) still referenced the column. From the teed
`slice-demo-run1.log` artifact:

```
=== 2/8 Run migrations ===
...
MIGRATION_ERROR file=182_embedding_dimension_768.sql
error=cannot drop column embedding of table iris_entities because other objects depend on it
SLICE DEMO FAILED: Migrations exited with code 1
```

## Resolution

Already fixed on main **before this remediation event fired**: commit `c94d2ce`
("fix(api): migration 182 — keep vector(1536) default and CASCADE drop to rebuild compat views")
changes the drop to `DROP COLUMN IF EXISTS embedding CASCADE` and recreates both compat views
with the same definitions.

Verified green: Slice Demo runs on the next three commits on main all completed **success**:

| Run | Head SHA | Conclusion |
|-----|----------|------------|
| 27310178852 | `c94d2ce` | success |
| 27311374666 | `4e03524` | success |
| 27311682766 | `1983681` | success |

No code change needed from this remediation; this report is the only artifact.
