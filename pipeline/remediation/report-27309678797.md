# Remediation Report — Run 27309678797 (Load Testing)

- **Date:** 2026-06-10
- **Head SHA:** 42a8de17078b0c95364d7d75fecfc965346d8743
- **Failed step:** Integration Load Tests → Run database migrations
- **Attempt:** 1 of 3

## Root cause

Migration `apps/api/migrations/182_embedding_dimension_768.sql` at the failing SHA dropped
`iris_entities.embedding` **without `CASCADE`** while the compat views from migration 180
(`indexed_entities`, `entity_vectors`) still referenced the column:

```
MIGRATION_ERROR file=182_embedding_dimension_768.sql
error=cannot drop column embedding of table iris_entities because other objects depend on it
```

## Resolution

Already fixed on main **before this remediation event fired**: commit `c94d2ce`
("fix(api): migration 182 — keep vector(1536) default and CASCADE drop to rebuild compat views")
changes the drop to `DROP COLUMN IF EXISTS embedding CASCADE` and recreates both compat views.

Verified green: Load Testing runs on `c94d2ce` and `4e03524` (the next two commits on main)
both completed **success**. No code change needed from this remediation; this report is the
only artifact.
