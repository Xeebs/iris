# Remediation Report — Run 27309176061 (Load Testing)

- **Date:** 2026-06-10
- **Head SHA:** 0b85cfaafb24535ab4a5398c468ec03362435216 (pre-fix)
- **Failed job/step:** Integration Load Tests / Run database migrations
- **Attempt:** 3 of 3 in window

## Symptom

Check-run annotations for the failed job show migrations aborting:

```
Last successful migration:  ran   181_enrichment_audit_log.sql
MIGRATION_ERROR file=182_embedding_dimension_768.sql error=cannot drop column embedding of table iris_entities because other objects depend on it
Migration failed (exit 1)
```

## Root cause — already fixed, stale run

At SHA 0b85cfa, migration `182_embedding_dimension_768.sql` ran
`ALTER TABLE iris_entities DROP COLUMN IF EXISTS embedding;` **without CASCADE**,
so the compat views `indexed_entities` / `entity_vectors` from migration 180
(which select the `embedding` column) blocked the drop.

The fix landed in **c94d2ce** (`fix(api): migration 182 — keep vector(1536)
default and CASCADE drop to rebuild compat views`): CASCADE drop plus
recreation of both views inside 182. c94d2ce is an ancestor of current HEAD
and is already on origin/main (local and remote are in sync; verified at
report time with `git fetch` + `git status -sb`).

This is the same defect already documented for the same pre-fix window in
`report-27309678793.md`, `report-27309467847.md`, and `report-27309467859.md`.
Run 27309176061 is simply another workflow (Load Testing) that executed
against a pre-fix SHA — no code change is needed or made.

## Action taken

None beyond this report. The verified-current `apps/api/migrations/182_embedding_dimension_768.sql`
on HEAD contains the CASCADE drop and view recreation; any Load Testing run on
c94d2ce or later will not hit this error.

## Suggested next probe (if this recurs on a post-fix SHA)

If `MIGRATION_ERROR` on 182 appears for a SHA that is a descendant of c94d2ce,
the dependency set has grown — query `pg_depend` for objects referencing
`iris_entities.embedding` in the failing environment and add them to the
recreate block in 182.
